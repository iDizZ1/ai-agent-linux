# 🤖 AI-Агент для управления Linux командами

![Status](https://img.shields.io/badge/status-beta-yellow) ![Python](https://img.shields.io/badge/python-3.9+-blue) ![License](https://img.shields.io/badge/license-MIT-green)

Интеллектуальный ассистент для генерации и безопасного выполнения bash-команд на основе описания на естественном языке (русский). Использует локальную LLM модель через Ollama.

**Статус:** Этап 1-2 завершён (базовая интеграция + безопасность + улучшенный UX)

---

## ✨ Возможности

### ✅ Реализовано (Этап 1-2)

**Генерация команд:**
- 🤖 AI-генерация bash команд через локальную LLM (Ollama)
- 💬 Работает с русским языком и понимает естественные описания задач
- 🔄 Два режима: одиночный запрос + интерактивный режим
- ⚡ Быстрая работа на локальной модели (2-4 токена/сек)

**Многоуровневая безопасность:**
- 🔒 Категоризация команд (безопасные / изменяющие файлы / опасные / критические)
- 🛑 Ранняя блокировка опасных команд ДО запроса подтверждения
- 📋 Whitelist из 50+ безопасных утилит (ls, cat, find, grep и т.д.)
- ⚠️ Расширенный список опасных паттернов (28+ паттернов)
- 🚫 Блокировка: `rm -rf /`, `dd if=`, fork bombs, рекурсивные удаления, скачивания

**Улучшенный интерфейс:**
- 🎨 Цветной вывод (зелёный/жёлтый/красный статусы)
- 💡 Объяснение каждой команды
- 📝 Встроенная справка (`help` команда)
- 📊 История текущей сессии (`history` команда)
- 🧹 Очистка экрана (`clear` команда)
- 🔌 Graceful shutdown (Ctrl+C)

**Надёжность:**
- 📝 Полное логирование в файл + консоль
- ⏱️ Timeout защита от бесконечных циклов (default 30s)
- 🔍 Улучшенный парсер с 4 способами извлечения команды
- 🛡️ Обработка ошибок и edge cases

---

## 📋 Требования

- Python 3.9+
- 8 GB RAM минимум
- Linux / macOS / Windows (WSL2)
- Ollama установлена

---

## 🚀 Быстрый старт за 5 минут

### 1️⃣ Установите и запустите Ollama

```bash
# Linux
curl -fsSL https://ollama.ai/install.sh | sh

# Запустите Ollama (в отдельном терминале)
ollama serve

# В другом терминале - загрузите модель
ollama pull llama3.2:3b
```

### 2️⃣ Подготовьте проект

```bash
# Клонируйте или перейдите в проект
cd ~/ai-agent-linux

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate          # Linux/macOS
# или
venv\Scripts\activate              # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 3️⃣ Запустите ассистента

**Одиночный запрос:**
```bash
python src/aiask.py ask "создать папку documents"
```

**Интерактивный режим:**
```bash
python src/aiask.py
```

---

## 📖 Примеры использования

### Одиночные запросы

```bash
# Показать список файлов
python src/aiask.py ask "покажи файлы в текущей папке"
🤖 Команда: ls -la
💡 Объяснение: Выведет список всех файлов с подробной информацией

# Найти файлы
python src/aiask.py ask "найди все python файлы"
🤖 Команда: find . -name "*.py"
💡 Объяснение: Поиск всех файлов с расширением .py в текущей директории

# Проверить свободное место
python src/aiask.py ask "сколько свободного места на диске"
🤖 Команда: df -h
💡 Объяснение: Показывает использование дискового пространства в удобном формате
```

### Интерактивный режим

```bash
$ python src/aiask.py

🤖 Запущен интерактивный режим AI-ассистента
💬 Введите 'exit' или 'quit' для выхода
📝 Введите 'help' для получения помощи
📊 Введите 'history' для просмотра истории текущей сессии
--------------------------------------------------

AIask> создать папку documents
🤖 Команда: mkdir documents
💡 Объяснение: Создание директории с именем documents
Выполнить? [y/N]: y
✅ Команда выполнена успешно

AIask> переди туда
🤖 Команда: cd documents
💡 Объяснение: Переход в директорию documents
Выполнить? [y/N]: y
✅ Команда выполнена успешно

AIask> где я сейчас
🤖 Команда: pwd
💡 Объяснение: Вывод текущей рабочей директории
Выполнить? [y/N]: y
✅ Команда выполнена успешно
/home/user/ai-agent-linux/documents

AIask> help
(выводится подробная справка)

AIask> history
(выводится история текущей сессии)

AIask> exit
👋 До свидания!
```

---

## 🔒 Система безопасности

### Категории команд

| Категория | Иконка | Примеры | Обработка |
|-----------|--------|----------|-----------|
| **Безопасные** | ✓ | ls, cat, pwd, find | Выполняются сразу |
| **Изменяющие** | ✎ | mkdir, touch, cp | Требуют подтверждения |
| **Опасные** | ⚠ | rm, chmod, kill | Требуют подтверждения |
| **Критичные** | ⛔ | rm -rf /, mkfs | Блокируются сразу |

### Блокируемые команды

Автоматически блокируются без запроса:
- `rm -rf /` и вариации (удаление корня)
- `dd if=` (запись на диски)
- `mkfs`, `fdisk` (форматирование)
- Fork bombs (`:(){` и т.д.)
- Скачивание (`wget http`, `curl http`)
- Pipe в shell (`| sh`, `| bash`)
- Системные команды (`shutdown`, `reboot`)

---


## 📝 Логирование

**В файл (`logs/aiask.log`):**
- Все уровни: DEBUG, INFO, WARNING, ERROR
- Полная детализация всех операций
- История всех команд

**В консоль:**
- WARNING и выше (опасные команды, ошибки)
- Детали парсинга при ошибках

Пример логов:
```
2025-10-27 01:00:00,123 - __main__ - INFO - Запуск в режиме одиночного запроса
2025-10-27 01:00:01,456 - llm_client - INFO - LLM ответ: mkdir documents
2025-10-27 01:00:02,789 - executor - INFO - Попытка выполнения [write]: mkdir documents
2025-10-27 01:00:02,890 - executor - INFO - Команда выполнена [код 0]
2025-10-27 01:00:10,234 - executor - WARNING - 🚨 Обнаружен опасный паттерн 'rm -rf /'
```

---

## 🛠️ Развёртывание

### Локально (для разработки)

```bash
git clone <repo>
cd ai-agent-linux
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python src/aiask.py
```

### Docker (опционально, в будущем)

```bash
docker build -t ai-agent-linux .
docker run -it ai-agent-linux
```

---

## 🐛 Решение проблем

### "Connection refused localhost:11434"
Убедитесь, что Ollama запущена:
```bash
ollama serve
```

### "ModuleNotFoundError: No module named 'typer'"
Установите зависимости:
```bash
pip install -r requirements.txt
```

### "Model not found"
Загрузите модель:
```bash
ollama pull qwen3:1.7b
```

### Медленная генерация
Первый запрос медленнее (10-15 сек). Это нормально для локальной модели.
Переключитесь на меньшую модель:
```python
model_name: str = "llama3.2:3b"  # Вместо более крупной
```

### Парсинг не извлекает команду
Проверьте логи:
```bash
tail -f logs/aiask.log
```
Там будет информация о том, почему не удалось разобрать ответ.

---


